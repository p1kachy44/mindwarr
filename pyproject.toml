[tool.poetry]
name = "telegram-motivation-bot"
version = "0.1.0"
description = "A Telegram bot that delivers daily morning motivation and evening reflection prompts for personal growth"
authors = ["Replit User <replituser@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
python-telegram-bot = "^20.4"
flask = "^2.3.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"